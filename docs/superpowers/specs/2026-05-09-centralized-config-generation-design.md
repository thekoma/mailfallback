# Centralized Config Generation + Modular Components

## Problem

Dovecot and Roundcube configs are static files in the repo (`docker/dovecot/conf.d/`, `docker/roundcube/custom.php`), manually maintained and volume-mounted. Adding a new component (Tika) means more static files. Configuration is scattered across env vars, compose files, and config files.

## Solution

MFB becomes the single source of truth for all component configuration. At startup, MFB generates config files into named volumes that each component mounts at its expected path. Feature flags control which components are active.

## Architecture

### Volume Layout

One named volume per component. MFB writes, components read.

```
dovecot_confd volume:
  MFB mounts as    → /confs/dovecot
  Dovecot mounts as → /etc/dovecot/conf.d:ro

  Contents (generated by MFB):
    mfb-auth.conf
    mfb-mail.conf
    mfb-acl.conf
    mfb-fts.conf
    mfb-service.conf
    mfb-ssl.conf
    mfb-stats.conf
    mfb-lua-userdb.lua
    dovecot-acl

webmail_conf volume:
  MFB mounts as      → /confs/webmail
  Roundcube mounts as → /var/roundcube/config:ro

  Contents (generated by MFB):
    custom.php
```

### Components and Feature Flags

| Component | Flag | Default | Effect |
|-----------|------|---------|--------|
| Dovecot | always on | - | Config always generated |
| Webmail | `MAILFALLBACK_WEBMAIL_ENABLED` | `false` | Generates `custom.php`, shows Webmail link in nav |
| Tika | `MAILFALLBACK_TIKA_ENABLED` | `false` | Adds `fts_tika` directive to Dovecot FTS config |

Tika has no config volume — it's stateless. When enabled, MFB adds `fts_tika = http://tika:9998/tika/` to the Dovecot FTS config file.

### New Settings

```python
# config.py additions
tika_enabled: bool = False
tika_url: str = "http://tika:9998"
webmail_enabled: bool = False
webmail_imap_host: str = "dovecot"
webmail_imap_port: int = 31143
confs_path: str = "/confs"
```

`dovecot_enabled` is removed — Dovecot is always on.

### Config Generator Service

New file: `services/config_generator.py`

```python
def generate_all_configs(settings) -> None:
    """Called at app startup. Generates all component configs."""
    generate_dovecot_config(settings)
    if settings.webmail_enabled:
        generate_webmail_config(settings)

def generate_dovecot_config(settings) -> None:
    """Writes 9 files to {confs_path}/dovecot/"""

def generate_webmail_config(settings) -> None:
    """Writes custom.php to {confs_path}/webmail/"""

def reload_after_config_change(settings) -> None:
    """Regenerate changed config + call doveadm reload"""
```

Templates are Python f-strings (same pattern as `generate_mbsyncrc`). Each config file has a dedicated function that returns the file content as a string.

### Startup Flow

In `app.py` lifespan, before existing startup tasks:

```
1. ensure_confs_directories()     # mkdir -p /confs/dovecot, /confs/webmail
2. generate_all_configs(settings)  # write all config files
3. (existing startup: ensure_default_store, ensure_admin, etc.)
```

### Config Reload on Settings Change

When an admin changes a setting via UI/API:
1. MFB rewrites the affected config file(s) in the volume
2. For Dovecot: calls `reload_dovecot()` (existing function, doveadm HTTP API)
3. For Roundcube: no action needed (PHP reads config on each request)

### Docker Compose Changes

```yaml
volumes:
  dovecot_confd:
  webmail_conf:
  maildirs:
  maildirs2:

services:
  mailfallback:
    volumes:
      - dovecot_confd:/confs/dovecot
      - webmail_conf:/confs/webmail
      - maildirs:/data/mailboxes
      - maildirs2:/data/mailboxes2

  dovecot:
    image: dovecot/dovecot:latest-2.4
    volumes:
      - dovecot_confd:/etc/dovecot/conf.d:ro
      - maildirs:/data/mailboxes
      - maildirs2:/data/mailboxes2
    ports:
      - "31143:31143"
      - "9900:9900"
    environment:
      DOVECOT_API_KEY: ${MAILFALLBACK_DOVECOT_API_KEY:-mfb-dovecot-api-key-change-me}
    depends_on:
      db:
        condition: service_healthy
      mailfallback:
        condition: service_healthy

  roundcube:
    image: roundcube/roundcubemail:latest
    volumes:
      - webmail_conf:/var/roundcube/config:ro
    ports:
      - "8001:80"
    depends_on:
      dovecot:
        condition: service_started
    profiles:
      - webmail

  tika:
    image: apache/tika:latest
    ports:
      - "9998:9998"
    profiles:
      - tika
```

Key changes:
- `docker/dovecot/conf.d/` bind mounts replaced by `dovecot_confd` named volume
- `docker/roundcube/custom.php` bind mount replaced by `webmail_conf` named volume
- Roundcube uses `profiles: [webmail]` — only starts with `docker compose --profile webmail up`
- Tika uses `profiles: [tika]` — only starts with `docker compose --profile tika up`
- Dovecot env vars reduced — DB credentials now embedded in generated config by MFB
- Dovecot depends on MFB being healthy (configs must exist before Dovecot reads them)

### What Gets Deleted from Repo

```
docker/dovecot/conf.d/mfb-auth.conf      → template in config_generator.py
docker/dovecot/conf.d/mfb-mail.conf      → template in config_generator.py
docker/dovecot/conf.d/mfb-acl.conf       → template in config_generator.py
docker/dovecot/conf.d/mfb-fts.conf       → template in config_generator.py
docker/dovecot/conf.d/mfb-service.conf   → template in config_generator.py
docker/dovecot/conf.d/mfb-ssl.conf       → template in config_generator.py
docker/dovecot/conf.d/mfb-stats.conf     → template in config_generator.py
docker/dovecot/conf.d/mfb-lua-userdb.lua → template in config_generator.py
docker/dovecot/dovecot-acl               → template in config_generator.py
docker/roundcube/custom.php              → template in config_generator.py
```

The `docker/` directory retains only `Dockerfile` and `entrypoint.sh` for MFB.

### dovecot-acl Path

The `dovecot-acl` file is placed inside `conf.d/` (same volume). The generated `mfb-acl.conf` references it with absolute path:

```
acl_global_path = /etc/dovecot/conf.d/dovecot-acl
```

This works because Dovecot accepts any absolute path for `acl_global_path`.

### Tika Integration

When `MAILFALLBACK_TIKA_ENABLED=true`, the generated `mfb-fts.conf` includes:

```
fts_tika = http://tika:9998/tika/
```

This tells Dovecot's FTS engine to send attachments to Tika for text extraction during indexing. Tika is stateless — no config file needed, just the running container.

The Tika URL is configurable via `MAILFALLBACK_TIKA_URL` (default: `http://tika:9998`).

## Environment Variable Strategy

### Principle

`.env` in the project root is the single source of truth. Docker Compose reads it automatically for `${}` interpolation in the YAML. Only MFB receives the full `.env` as container environment (`env_file: .env`). All other services receive only the minimum env vars they need via explicit `environment:` in compose — values interpolated from the same `.env`.

### Per-Service Breakdown

| Service | `env_file` | `environment:` vars | Why |
|---------|-----------|---------------------|-----|
| **db** | no | `POSTGRES_USER/PASSWORD/DB` (from `${DB_*}`) | PostgreSQL bootstrap requires these at init |
| **MFB** | `.env` | `MAILFALLBACK_DATABASE_URL` (from `${DB_*}`) | Control plane, needs all settings |
| **Dovecot** | no | `DOVEADM_PASSWORD` (from `${MAILFALLBACK_DOVECOT_API_KEY}`) | Only env var the stock `dovecot.conf` reads via `$ENV:`. Everything else is in generated config files |
| **Roundcube** | no | `ROUNDCUBEMAIL_DB_*` (from `${DB_*}`), `ROUNDCUBEMAIL_DEFAULT_HOST/PORT` | Entrypoint script uses these at boot to generate `config.inc.php` and wait for DB. Cannot be replaced by mounted config. OAuth vars go in `custom.php` instead |
| **Tika** | no | none | Stateless, zero config |

### What Moves from Env to Generated Config

**Dovecot** (7 env vars eliminated):
- `DOVECOT_DB_HOST/PORT/NAME/USER/PASSWORD` → embedded in generated `mfb-auth.conf`
- `DOVECOT_OAUTH2_USERINFO_URL` → embedded in generated `mfb-auth.conf`
- `MFB_USERDB_URL` → embedded in generated `mfb-lua-userdb.lua`

Only `DOVEADM_PASSWORD` remains because the base `dovecot.conf` (inside the official image, not overridable via `conf.d/`) references `$ENV:DOVEADM_PASSWORD`.

**Roundcube** (OAuth vars eliminated):
- `ROUNDCUBE_OAUTH_CLIENT_ID/SECRET/AUTH_URI/TOKEN_URI/IDENTITY_URI` → embedded in generated `custom.php`

DB vars (`ROUNDCUBEMAIL_DB_*`) and IMAP host/port stay as env vars because Roundcube's entrypoint script processes them before PHP starts.

### Resulting `.env`

```env
# Database (shared by db, MFB, Roundcube via compose interpolation)
DB_USER=mailfallback
DB_PASSWORD=mailfallback
DB_NAME=mailfallback

# MFB core
MAILFALLBACK_SECRET_KEY=change-me
MAILFALLBACK_SESSION_SECRET=change-me
MAILFALLBACK_DOVECOT_API_KEY=mfb-dovecot-api-key-change-me

# Modules
MAILFALLBACK_WEBMAIL_ENABLED=true
MAILFALLBACK_TIKA_ENABLED=false

# OIDC (optional)
MAILFALLBACK_OIDC_ENABLED=false
# MAILFALLBACK_OIDC_CLIENT_ID=...
# MAILFALLBACK_OIDC_CLIENT_SECRET=...
# MAILFALLBACK_OIDC_DISCOVERY_URL=...

# OAuth providers (optional)
# MAILFALLBACK_GOOGLE_CLIENT_ID=...
# MAILFALLBACK_GOOGLE_CLIENT_SECRET=...
# MAILFALLBACK_MICROSOFT_CLIENT_ID=...
# MAILFALLBACK_MICROSOFT_CLIENT_SECRET=...
```

### Resulting Compose

```yaml
services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: ${DB_USER:-mailfallback}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-mailfallback}
      POSTGRES_DB: ${DB_NAME:-mailfallback}

  mailfallback:
    env_file: .env
    environment:
      MAILFALLBACK_DATABASE_URL: postgresql://${DB_USER:-mailfallback}:${DB_PASSWORD:-mailfallback}@db:5432/${DB_NAME:-mailfallback}
    volumes:
      - dovecot_confd:/confs/dovecot
      - webmail_conf:/confs/webmail
      - maildirs:/data/mailboxes
      - maildirs2:/data/mailboxes2

  dovecot:
    image: dovecot/dovecot:latest-2.4
    environment:
      DOVEADM_PASSWORD: ${MAILFALLBACK_DOVECOT_API_KEY:-mfb-dovecot-api-key-change-me}
    volumes:
      - dovecot_confd:/etc/dovecot/conf.d:ro
      - maildirs:/data/mailboxes
      - maildirs2:/data/mailboxes2

  roundcube:
    image: roundcube/roundcubemail:latest
    environment:
      ROUNDCUBEMAIL_DB_TYPE: pgsql
      ROUNDCUBEMAIL_DB_HOST: db
      ROUNDCUBEMAIL_DB_PORT: "5432"
      ROUNDCUBEMAIL_DB_USER: ${DB_USER:-mailfallback}
      ROUNDCUBEMAIL_DB_PASSWORD: ${DB_PASSWORD:-mailfallback}
      ROUNDCUBEMAIL_DB_NAME: ${DB_NAME:-mailfallback}
      ROUNDCUBEMAIL_DEFAULT_HOST: dovecot
      ROUNDCUBEMAIL_DEFAULT_PORT: "31143"
      ROUNDCUBEMAIL_SKIN: elastic
      ROUNDCUBEMAIL_PLUGINS: archive,zipdownload,subscriptions_option
    volumes:
      - webmail_conf:/var/roundcube/config:ro
    profiles:
      - webmail

  tika:
    image: apache/tika:latest
    profiles:
      - tika
```

## Testing

- Unit tests for `config_generator.py`: verify each generated config contains expected values, respects feature flags
- Test Tika flag adds `fts_tika` to FTS config
- Test webmail flag generates `custom.php`
- Integration: `docker compose up` with new volumes, verify Dovecot reads generated config via `doveconf -n`

## Migration Path

1. Deploy new code — MFB generates configs at boot
2. Old bind-mounted configs are overridden by the new volume mount
3. Remove old `docker/dovecot/conf.d/` and `docker/roundcube/custom.php` from repo
4. No database migration needed

## Files to Create/Modify

**New:**
- `src/mailfallback/services/config_generator.py`
- `tests/test_config_generator.py`

**Modified:**
- `src/mailfallback/config.py` — add tika/webmail/confs settings, remove dovecot_enabled
- `src/mailfallback/app.py` — call config generator at startup
- `docker-compose.yml` — new volume layout, profiles for optional components
- `src/mailfallback/routers/ui.py` — use webmail_enabled instead of webmail_url for nav visibility
- `src/mailfallback/templates/base.html` — webmail link conditional on webmail_enabled

**Deleted:**
- `docker/dovecot/conf.d/*` (10 files)
- `docker/roundcube/custom.php`
